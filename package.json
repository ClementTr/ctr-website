{
  "name": "routerapp",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "leaflet": "^1.9.1",
    "react": "^16.10.1",
    "react-dom": "^16.10.1",
    "react-router-dom": "^5.1.1",
    "react-scripts": "^5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "deploy": "aws --profile user-clement s3 sync build/ s3://clementtailleur.com",
    "cache": "aws --profile user-clement cloudfront create-invalidation --distribution-id E2294DXPG9HTGF --paths '/*'",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
